export class Education {
  constructor({ name, date, description, projects } = {}) {
    this.name = name;
    this.date = date;
    this.description = description;
    this.projects = projects;
  }
}
